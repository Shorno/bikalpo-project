{
  "name": "server",
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "build": "pnpm exec tsdown",
    "check-types": "tsc -b",
    "compile": "bun build --compile --minify --sourcemap --bytecode ./src/index.ts --outfile server",
    "dev": "bun run --cwd ../.. --env-file apps/server/.env --hot apps/server/src/index.ts",
    "start": "bun run dist/index.mjs"
  },
  "dependencies": {
    "@bikalpo-project/api": "workspace:*",
    "@bikalpo-project/auth": "workspace:*",
    "@bikalpo-project/db": "workspace:*",
    "@bikalpo-project/env": "workspace:*",
    "@orpc/openapi": "catalog:",
    "@orpc/server": "catalog:",
    "@orpc/zod": "catalog:",
    "@socket.io/bun-engine": "^0.1.0",
    "better-auth": "catalog:",
    "dotenv": "catalog:",
    "hono": "catalog:",
    "socket.io": "^4.8.3",
    "zod": "catalog:"
  },
  "devDependencies": {
    "@bikalpo-project/config": "workspace:*",
    "@types/bun": "catalog:",
    "tsdown": "^0.16.5",
    "typescript": "catalog:"
  }
}
